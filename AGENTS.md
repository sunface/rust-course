# 内容写作风格

本约定适用于全书正文的新增与修改。

## 核心原则

- 以完整作品和最终成稿的标准编写正文。每句话都服务于当前章节的教学目标和叙事，不把审计笔记、修改理由、考据过程、延伸联想或思考推理写入正文。
- 不自作主张。只修改用户明确确认的问题；发现疑问时，先报告证据和建议，未经确认不顺手扩写、删减、替换或重组正文。
- 修改旧内容时保留原有的方法、案例、幽默、表达和章节衔接，坚持最小修改：能改词就不改句，能改句就不改段。
- 允许增加能够补齐理解链条、提升实践价值或丰富阅读体验的相关内容。新增内容必须自然融入上下文、适合当前学习阶段并经过验证。非主线内容确有价值时，可以作为扩展阅读；不要加入无上下文的旁支知识或只为展示知识面的内容。

## 教学组织

- 以帮助读者理解和运用知识为首要目标。根据内容灵活选择问题、例子、现象、类比或原理作为切入点，循序渐进、深入浅出，不套用固定讲解顺序。
- 对仅靠文字难以讲清的内存关系、所有权变化、执行流程和状态转换等难点场景，优先考虑使用图解。图解必须围绕一个明确问题，并配合正文说明观察重点和结论。属于简化模型时，应说明适用范围，避免让读者误认为语言实现保证。
- 讲解相近概念时，先区分它们的层级和角色，不把数据的定义、所有权和使用方式混为一谈。例如，字符串字面量说明文本如何定义，`String` 说明谁拥有字符串数据，`&str` 说明如何借用已有数据。
- 一个段落或示例只讲一个核心关系，一句话只表达一个核心判断。条件、原因、边界和术语解释优先放到下一句或示例中；朗读不顺或需要回头寻找主语时，必须拆句。
- 修正错误时，将仍然有效的内容自然放回合适位置，不留下针对旧文的反驳或修改说明。
- 技术准确性必须服务于教学效果，不为追求形式完整而堆砌超出当前学习阶段的术语、边界情况和 API。能用读者已经掌握的词和具体例子讲清时，不引入新术语。
- 不用语言规范中的正式术语替换直观讲解。例如，不写“加上分号后变成表达式语句”，而写“加上分号后，表达式的值会被丢弃”。
- 示例代码使用有意义的英文命名；注释解释意图和原因，不逐行复述代码。
- 类比只用于帮助建立直觉，随后必须给出准确解释，并说明类比的适用边界。

## 语言表达

- 使用简体中文，以“你”与读者交流，以“我们”共同分析和实践。
- 保持亲切、直接、生动的作者风格，可以有克制的幽默。不要依赖网络梗、流行语或夸张表达，也不要把原有声线改成冷冰冰的技术手册。
- 避免公文腔、翻译腔和机械化的 AI 表达，不写空泛的开场、重复的总结或无意义的过渡句。
- 不使用“显然”“很简单”“众所周知”等可能给初学者造成压力的措辞。
- 结论能够确定时直接陈述；结论取决于条件时，紧接着说明具体条件。不要使用没有说明条件的“可能”“通常”“一般来说”“取决于具体情况”等模糊限定。相关边界超出当前章节时，只讲当前范围内能够确定的事实。
- 技术表述保持准确，明确区分语言保证、标准库约定和当前实现细节。涉及版本、生态现状或工具行为时写明具体版本或时间，不使用“最新版”“目前一直如此”等模糊说法。
- 专业概念首次出现时使用“中文名称（英文术语）”，后文保持同一种叫法。
- 标题应简洁、自然、有辨识度，不使用标题党式措辞。

## 格式与提交

- Rust 关键字、类型、函数、命令和文件名使用反引号，例如 `trait`、`String`、`cargo run`。
- 中文与英文、数字之间保留一个空格，例如“在 Rust 2024 中”；中文标点前后不额外加空格。
- Git commit 信息保持简洁，只用一句话概括本次修改，例如 `fix: typo in src/difficulties/slice.md`。
