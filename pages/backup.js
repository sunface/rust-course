import React from "react";

import AppLayout from "../components/AppLayout";

const backup = props => {
  return (
    <AppLayout>
      <div>Backup Page</div>
    </AppLayout>
  );
};

export default backup;
