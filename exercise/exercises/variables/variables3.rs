// variables3.rs
// 让我能够编译！执行 `rustex hint variables3` 获取提示 :)

// I AM NOT DONE

fn main() {
    let x = 3;
    println!("Number {}", x);// 译："数字 {}"
    x = 5; // don't change this line（译：不要更改这一行）
    println!("Number {}", x);
}
